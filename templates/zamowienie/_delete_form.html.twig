<form method="post" action="{{ path('app_zamowienie_delete', {'id': zamowienie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zamowienie.id) }}">
    <button class="btn">Delete</button>
</form>
